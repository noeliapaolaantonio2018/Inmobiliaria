@model Inmobiliaria.Models.Pagos

@{ ViewData["Title"] = "Crear"; }

<h2>Nuevo Pago</h2>

<h6 class="alert alert-info">  Contrato Nº: @ViewBag.Contratos.IdContr con el Inquilino: @ViewBag.Contratos.Inquilinos.Nombre  @ViewBag.Contratos.Inquilinos.Apellido .
    Por el Inmueble de tipo: @ViewBag.Contratos.Inmuebles.Tipo ubicado en: @ViewBag.Contratos.Inmuebles.Direccion</h6>

<hr />

<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group">
                <label asp-for="NumPago" class="control-label">Numero de Pago</label>
                <input asp-for="NumPago" value=@ViewBag.NumPago class="form-control" readonly />
                <span asp-validation-for="NumPago" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="IdContr" class="control-label">Nº Contrato</label>
                <input asp-for="IdContr" value=@ViewBag.Contratos.IdContr class="form-control" type="hidden" />
                <span asp-validation-for="IdContr" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="FechaPago" class="control-label">Fecha De Pago</label>
                <input asp-for="FechaPago" type="date" value="@DateTime.Today.ToString("yyyy-MM-dd")" class="form-control" readonly />
                <span asp-validation-for="FechaPago" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="Importe" class="control-label"></label>
                <input asp-for="Importe" value=@ViewBag.Contratos.Monto class="form-control" readonly />
                <span asp-validation-for="Importe" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Guardar" class="btn btn-success" />  |
                <a class="btn btn-info" asp-action="Index">Regresar Listado Pagos</a>
            </div>
        </form>
    </div>
</div>



